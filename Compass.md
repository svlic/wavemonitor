# 工程决策罗盘（Compass）

> 本文不是 WaveMonitor 的架构说明，而是从其 ADR、Git 历史和项目所有者复盘中提炼的未来项目决策指南。
> 它用于帮助新项目更早验证风险、建立低成本 Baseline、控制 AI 辅助开发产生的复杂度，并及时停止低收益投入。

## 1. 如何使用本文

新项目启动时，先完成“项目启动卡”和“启动检查清单”；遇到重要技术选择时使用“技术决策框架”；每个迭代结束时执行“方案退出检查”和“AI 代码消融检查”；项目准备收尾时使用“结束检查清单”。

本文对结论采用三种证据等级：

| 标记 | 含义 | 使用方式 |
| --- | --- | --- |
| **已验证经验** | 有 ADR、代码历史或项目所有者确认的实际经历支持 | 可作为默认决策，但仍需检查适用条件 |
| **复盘判断** | 基于事实形成的事后判断，因缺少指标或对照实验而不能视为定律 | 可用于提出约束或实验，不应直接当作结论 |
| **待验证假设** | 当前证据不足，只能作为未来项目要主动验证的问题 | 必须定义验证方法和停止条件 |

使用时必须保留这个区别。不要把“改完以后看起来更简单”写成性能收益，也不要把“测试通过”写成生产可靠性已经得到证明。

---

## 2. 核心工程原则

### 原则一：先验证决定方案生死的外部约束

**证据等级：已验证经验**

**来源**

- [ADR 0010：Cloudflare Workers 与 D1 部署实验](docs/adr/0010-cloudflare-workers-experiment.md)
- 项目所有者确认：Cloudflare 方案在投入完整实现前没有进行可行性实验；采用原因是免费托管，撤回的决定性原因是无法满足行情代理需求。

**背景**

项目为了免费托管，引入了第二套 TypeScript 后端、D1、Cron、数据迁移和 GUI 设置。完整方案进入主仓库后，才发现目标运行环境不支持项目访问受限行情源所需的代理能力。方案随后从主仓库撤回，转入独立仓库。

**关键教训**

决定技术方案能否成立的，往往不是常规功能，而是少数不可妥协的环境约束。若这些约束没有先被验证，功能完成度越高，沉没成本越大。

**行动指南**

1. 在比较技术方案前，列出 3～5 个“失败即淘汰”的约束，例如：
   - 必须访问的外部网络和代理方式；
   - 后台任务的时限、调度精度和并发模型；
   - 持久化、事务和备份能力；
   - 密钥、合规、地区或运行时限制；
   - 可接受的最低成本与运维方式。
2. 对每个候选方案执行最小的纵向实验，只验证这些约束，不先实现完整产品。
3. 实验必须连接真实约束：真实运行环境、真实网络路径和有代表性的外部服务。Mock 只能验证本地逻辑。
4. 在实验开始前写明通过标准、失败标准、时间预算和终止动作。
5. 只有全部致命约束通过后，才投入数据迁移、管理界面和第二套业务实现。

**适用条件**

- Serverless、托管平台、云服务、第三方 API、受限网络或新运行时选型；
- 需要复制现有系统到另一套技术栈；
- 方案价值主要来自成本、部署或平台能力，而非产品功能。

**例外与限制**

- 最小实验无法证明长期可靠性，只能尽早排除已知不可行方案。
- Cloudflare 案例证明了“跳过验证有风险”，但没有证明某一种具体实验设计必然足够。实验内容仍需按项目约束制定。
- 探索性原型允许失败，但必须限制预算，且不能未经决策就演变为生产双实现。

**验证方法**

- 实现正式功能前，每个致命约束都有可重复的实验记录；
- 被淘汰的方案消耗不超过事先约定的探索预算；
- 后续没有因实验本应覆盖的环境限制而整体撤回方案。

---

### 原则二：先建立与真实产品边界匹配的最小端到端 Baseline

**证据等级：已验证经验（边界有限）**

**来源**

- [ADR 0001：采用单实例全栈与轮询架构](docs/adr/0001-single-instance-full-stack-and-polling.md)
- [ADR 0008：在 FastAPI 生命周期内运行监控调度器](docs/adr/0008-in-process-monitoring-lifecycle.md)
- 项目所有者确认：项目实际运行过；若重新开始，目前没有必要更换 FastAPI、React、SQLite 和单进程轮询组合。

**背景**

WaveMonitor 面向个人或小团队单实例部署。项目选择一个 API 进程、SQLite、本地数据卷和轮询式行情接入，没有提前引入分布式调度、消息队列、PostgreSQL 或 WebSocket-first 架构。

**关键教训**

架构应服务已确认的产品边界，而不是想象中的未来规模。一个可部署、可观测、可触发核心结果的简单闭环，比提前建设扩展能力更适合作为后续决策的参照基线。

**行动指南**

1. 定义一条最短端到端路径：输入真实配置 → 获取真实数据 → 执行核心规则 → 产生用户可见结果。
2. Baseline 只承诺当前用户量、数据量和可用性要求，不预先承诺多租户、高可用或水平扩展。
3. 明确记录“不支持”的运行方式，例如多 worker、多实例或实时流语义。
4. 为未来升级定义触发条件，而不是提前实现升级方案：
   - 单实例资源达到持续阈值；
   - 可用性目标超过单故障域能力；
   - 重复请求造成可测量的配额或成本问题；
   - 用户隔离、审计或权限成为真实产品需求。
5. 后续优化必须与 Baseline 比较用户结果、复杂度和资源成本。

**适用条件**

- 用户和负载规模明确的小型工具、内部系统或个人服务；
- 首要目标是验证工作流与业务规则；
- 单机故障恢复和短暂中断可以接受。

**例外与限制**

- 如果项目从第一天就有明确的多租户、法规、高可用或大吞吐要求，单实例可能不是合格 Baseline。
- 本项目没有各组件的长期故障统计，不能据此断言该技术栈在所有类似项目中都可靠。
- 简单架构不等于忽略备份、密钥、迁移和故障恢复。

**验证方法**

- Baseline 能在目标环境完成真实端到端任务；
- 复杂组件的引入都能对应一个超过阈值的实际问题；
- 每次升级均能说明相对于 Baseline 改善了什么，以及增加了什么维护成本。

---

### 原则三：把告警定义为显式状态转换，而不是“条件 + 去重字段”

**证据等级：已验证经验**

**来源**

- [ADR 0007：使用持久化状态机定义告警与重试](docs/adr/0007-persistent-alert-state-machine.md)
- 相关历史经历：cooldown、条件边沿、永久一次、数据库 claim、rule cycle 和重新触发语义先后演进。
- 项目所有者确认：持续条件导致重复告警是实际运行反馈；后续变化主要源于需求变化。

**背景**

轮询系统会重复观察相同条件。“当前条件为真”不足以决定是否通知，还必须知道此前状态、何时解除、配置是否变化、发送是否成功，以及服务重启后如何恢复。WaveMonitor 的多次修改说明，这些语义无法只靠事件唯一键或冷却时间表达。

**关键教训**

告警的核心不是布尔判断，而是业务状态转换。需求变化本身并不代表早期设计错误；真正的风险是状态语义没有集中表达，导致每次变化都通过新字段或补丁叠加。

**行动指南**

1. 在实现前写状态转换表，至少覆盖：

   | 当前状态 | 输入/事件 | 下一状态 | 是否产生业务事件 | 是否尝试投递 |
   | --- | --- | --- | --- | --- |
   | 未触发 | 条件进入 | 已触发 | 是 | 是 |
   | 已触发 | 条件持续 | 已触发 | 否 | 否 |
   | 已触发 | 条件解除 | 可再次触发 | 否 | 否 |
   | 待投递 | 投递失败 | 待重试或终止 | 否 | 依策略 |
   | 任意 | 规则语义变化 | 新规则周期 | 依新规则 | 依策略 |

2. 分开定义四件事：
   - 条件是否成立；
   - 业务事件是否首次发生；
   - 通知是否成功投递；
   - 何时允许再次触发。
3. 用不对称边界样本测试：首次观测、恰好等于阈值、持续命中、离开后重入、编辑配置、重启、发送失败。
4. 真实运行后，根据误报、漏报和重复通知修订状态表；不要只修单个失败测试。
5. 每次需求变化时，先更新状态表和事件语义，再修改持久化字段。

**适用条件**

- 告警、通知、审批、重试、同步、任务编排等跨时间业务；
- 条件可能持续成立，且需要 reset/re-arm；
- 进程重启后仍需保持语义。

**例外与限制**

- 一次性、无重试、无状态的通知不需要完整状态机。
- 状态转换表不能消除需求变化，只能让变化影响更清晰。
- 多实例 exactly-once 需要额外的一致性机制；本项目只验证了单调度器边界。

**验证方法**

- 为每个状态转换至少有一个行为测试；
- 持续条件不会重复通知，解除后能按规则再次通知；
- 配置变化和投递失败的行为有明确、可观察的结果；
- 生产反馈可以映射到状态表中的具体转换，而不是无法解释的字段组合。

---

### 原则四：新方案合入时，旧方案必须有明确退出动作

**证据等级：已验证经验**

**来源**

- [ADR 0009：在前端 API 边界校验数据并定时刷新](docs/adr/0009-frontend-api-validation-and-polling.md)
- [ADR 0012：避免没有独立职责的薄抽象层](docs/adr/0012-avoid-redundant-thin-layers.md)
- [ADR 0013：删除遗留行情观测持久化结构](docs/adr/0013-remove-legacy-price-observation-storage.md)
- 项目所有者确认：技术债主要来自新方案加入后没有回收旧方案。

**背景**

项目更换过前端刷新方式、告警 re-arm 方案和最新行情存储边界。部分旧 helper、store、hook、ORM 模型、表和迁移路径在新方案运行后仍被保留，直到后期消融式重构才删除。

**关键教训**

技术债不只来自错误设计，也来自已完成的迁移没有完成退出。若“新增替代方案”与“删除旧方案”分离，旧代码很容易被误认为仍有兼容职责，并持续增加理解和测试成本。

**行动指南**

1. 每个替换性变更都附带一份退出清单：
   - 旧运行路径；
   - 旧状态和数据模型；
   - 旧配置、API 字段和兼容层；
   - 旧测试、文档和迁移逻辑；
   - 遗留数据的保留或删除策略。
2. 能在同一变更中安全删除的内容立即删除。
3. 暂时不能删除的内容必须写明负责人、截止条件和可观察的删除触发器。
4. 兼容字段应标注真实语义和废弃计划，不能仅因为“可能有人使用”而永久保留。
5. 合并前搜索旧符号和旧数据路径，确认新旧方案不会同时拥有同一职责。

**适用条件**

- 存储迁移、状态管理替换、API 版本切换、基础设施迁移；
- 需求变化导致领域模型或业务语义改变；
- AI 在多个迭代中连续修改同一功能。

**例外与限制**

- 对外兼容、灰度发布或数据回滚可能要求新旧方案短期共存。
- 删除不可逆历史数据前必须提供备份或迁移说明。
- “只有一个调用方”不是删除模块的充分理由；真正边界仍应保留。

**验证方法**

- 替换完成后，不再有旧运行路径的生产调用；
- 新数据库不会继续创建废弃结构；
- 兼容层数量有清单、有期限，且不会只增不减；
- 后续修改同一职责时，不需要同时理解两套方案。

---

### 原则五：把抽象放在真实变化边界，并为 AI 生成代码设置抽象预算

**证据等级：已验证经验 + 复盘判断**

**来源**

- [ADR 0002：通过适配器和供应商目录统一行情源](docs/adr/0002-market-data-adapters-and-provider-catalogs.md)
- [ADR 0012：避免没有独立职责的薄抽象层](docs/adr/0012-avoid-redundant-thin-layers.md)
- 项目所有者确认：后期清理与 AI 开发产生的代码膨胀和未回收旧方案有关。

**背景**

行情供应商确实存在 symbol、市场类型、SDK 数据形态和网络配置差异，因此适配器边界有稳定职责。相反，部分只承载类型、单条查询、参数转发、`setInterval` 或一次性页面数据的模块只增加了跳转。

**关键教训**

抽象的价值来自隔离变化，而不是文件数量、命名完整度或未来可能复用。AI 很容易生成局部上合理、整体上冗余的层，因此“能否生成一个抽象”必须转换为“这个抽象隔离了哪一种已发生的变化”。

**行动指南**

仅在至少满足一项时增加独立抽象：

- 有两个以上真实调用方，且共享行为而非碰巧共享几行代码；
- 隔离外部 SDK、网络、文件、数据库或框架副作用；
- 承载可独立说明、演进和测试的领域规则；
- 明显降低所有者模块的认知复杂度；
- 已观察到变化轴，需要阻止其扩散。

对 AI 生成的每个新模块、helper、hook、store 或接口，评审时回答：

1. 它拥有的职责是什么？
2. 删除它并内联后，会失去什么边界？
3. 当前有几个调用方？未来调用方是否只是推测？
4. 它是否重复了已有所有者？
5. 本次需求替换了什么，旧层是否同步删除？

每个里程碑执行一次“消融检查”：假设删除该层，测试和可读性是否反而改善。消融是评审方法，不是机械删除小文件。

**适用条件**

- AI 辅助或多代理开发；
- 快速迭代、任务被拆成多轮实现；
- 代码库出现大量一调用方 wrapper、type-only 文件或自建微型框架。

**例外与限制**

- 安全边界、API 入站校验、外部服务适配器和纯领域计算即使只有一个调用方，也可能值得独立。
- 本项目没有精确 Token 或维护时间数据，因此“抽象预算降低 Token”属于待验证效果。
- 不应为了减少文件数量制造大型所有者模块。

**验证方法**

- 新抽象在评审中能说明独立职责或真实变化轴；
- 功能替换后，相关模块总数和概念数不会持续单向增长；
- 新成员或新会话定位行为时，需要跨越的转发层减少；
- 消融检查发现的冗余数量随项目成熟而下降。

---

### 原则六：按恢复、审计和查询需求决定持久化，不按“以后可能有用”决定

**证据等级：复盘判断**

**来源**

- [ADR 0011：将最新行情状态保存在进程内存](docs/adr/0011-in-memory-latest-price-state.md)
- [ADR 0013：删除遗留行情观测持久化结构](docs/adr/0013-remove-legacy-price-observation-storage.md)
- 项目所有者确认：转为内存主要是“感觉可以优化”，没有已确认的性能故障或量化收益。

**背景**

初版把每次行情成功和错误写入 SQLite，并清理三天前的数据；后来只保留内存中的最新状态，再删除无人读取的历史观测表。告警历史、规则状态和投递记录则继续持久化，因为它们参与解释、去重和恢复。

**关键教训**

是否持久化应由数据丢失后果和查询需求决定。删除无需求的数据路径可以简化系统，但若没有前后指标，就不能宣称它已经带来性能收益。

**行动指南**

为每类状态回答：

1. 进程重启后丢失是否可接受？
2. 是否参与业务恢复、去重、审计或合规？
3. 是否存在已定义的历史查询？谁会使用？
4. 写入频率、保留期和容量上限是多少？
5. 数据删除是否需要用户通知或备份？

据此分类：

- **必须持久化**：丢失会改变业务正确性或审计事实；
- **可重建状态**：可从权威数据重新计算，并有可接受恢复时间；
- **瞬时状态**：重启后等待下一次更新即可；
- **尚无需求的历史数据**：默认不收集，先定义产品用途和保留成本。

若以性能优化为理由改变边界，改动前后至少记录写入量、数据库增长、查询延迟或运维故障之一。

**适用条件**

- 高频观测、缓存、运行状态、事件历史和审计数据；
- 嵌入式数据库或受限存储环境；
- 从持久化状态迁移到缓存或内存状态。

**例外与限制**

- 法规、安全和审计要求可能要求保留当前产品不直接读取的数据。
- 未来确实可能需要历史分析，但应先定义查询、保留期和成本，而非无限期保存。
- 本案例只证明当前产品接受重启后短暂空状态，不证明行情历史通常没有价值。

**验证方法**

- 每张表或状态存储都有明确的恢复、审计或查询用途；
- 重启演练符合声明的数据丢失边界；
- 历史数据保留量有上限；
- 性能优化结论由改动前后指标支持，否则标记为未验证。

---

### 原则七：在当前规模有效的自建机制，要同时定义升级触发器

**证据等级：有限验证**

**来源**

- [ADR 0005：由应用启动流程管理 SQLite 迁移](docs/adr/0005-application-managed-sqlite-migrations.md)
- 项目所有者确认：当前迁移方式没有造成明显负担或故障。

**背景**

项目没有引入 Alembic，而是在启动时识别已知 SQLite 结构并执行加列或重建表。该方式满足了当前单数据库、有限历史版本和单容器启动需求，但迁移逻辑逐渐集中在 `db.py`。

**关键教训**

局部自建机制可以是合理的简单方案，但“当前没有出问题”不等于可无限扩展。接受自建方案时必须同时记录它的容量边界和迁移出口。

**行动指南**

1. 采用自建机制前，明确它比成熟工具省掉的真实步骤。
2. 为每个受支持旧结构编写升级回归测试和重复执行测试。
3. 记录破坏性迁移、备份要求和失败恢复方法。
4. 出现以下任一情况时重新评估版本化工具：
   - 无法可靠识别中间 schema；
   - 需要维护多条升级路径；
   - 引入第二种数据库；
   - 多实例可能并发迁移；
   - 迁移需要回滚、审计或独立发布；
   - 迁移代码成为高频故障或主要维护负担。

**适用条件**

- 单实例、单数据库、升级路径数量有限；
- 可以在应用启动前备份；
- 迁移由完整自动化测试覆盖。

**例外与限制**

- 团队已有成熟迁移工具和流程时，自建方案未必更简单。
- 高可用、多实例和受监管系统不应仅依赖启动时特征检测。
- 本项目没有对 Alembic 做历史对照评估，因此不能得出“手写迁移更优”。

**验证方法**

- 所有实际发布过的 schema 均有升级测试；
- 迁移可重复执行且失败不会留下未知半状态；
- 每个里程碑统计迁移分支数量，并检查是否达到升级触发器。

---

### 原则八：ADR 只有在实现、测试和运行语义一致时才算完成

**证据等级：已验证风险**

**来源**

- [ADR 0007：使用持久化状态机定义告警与重试](docs/adr/0007-persistent-alert-state-machine.md)
- 当前 ADR 明确记录：期望 Telegram 失败后重试，但实现和测试仍锁定“失败消耗本次告警”。

**背景**

项目已经确认目标业务语义，却尚未完成实现对齐。若只阅读 ADR，会误以为失败重试是运行时保证；若只阅读测试，又会把临时偏差误认为最终需求。

**关键教训**

决策记录是意图，不是交付证明。已接受但尚未实现的决策必须显式标记，且不能在用户文档中描述为现有能力。

**行动指南**

1. ADR 状态至少区分：提议、已接受、实现中、已对齐、已取代。
2. 每项决策列出可执行的对齐证据：测试、迁移、运行检查或监控指标。
3. 行为 ADR 修改后，在同一任务中检查代码、契约、测试和用户文档。
4. 已知偏差建立单独清单，说明当前行为、目标行为、风险和关闭条件。
5. 发布说明只陈述已经验证的运行行为。

**适用条件**

- 行为语义、安全、数据迁移、重试和一致性决策；
- 从历史代码重建 ADR；
- 文档和实现由不同人员或不同 AI 会话维护。

**例外与限制**

- 探索性 ADR 可以暂不实现，但必须保持“提议”状态。
- 并非所有代码变化都需要 ADR；局部实现细节仍应由测试和代码说明。

**验证方法**

- 每个已接受 ADR 都能指向至少一项运行或自动化证据；
- ADR、契约和测试对同一边界给出相同答案；
- 已知偏差不会长期无负责人、无关闭条件地存在。

---

### 原则九：项目开始时定义成功和停止条件；无法定义时，把探索预算写清楚

**证据等级：待验证假设**

**来源**

- 初始计划清楚定义了功能范围和“不做什么”，但本次复盘无法确认项目最终完成标准、各部分长期可靠性或除已知事项外的低价值投入。
- 项目所有者对这些问题明确回答“不确定”。

**背景**

功能范围并不等于成功标准。项目可以不断增加功能、优化或重构，却没有一个明确时点判断核心目标是否已经实现、是否应该转入维护。

**关键教训**

本项目证据不足以证明曾发生“无限扩张”，但无法回答结束条件本身就是未来项目应避免的信息缺口。新的项目应让成功、失败和停止都可以被判断。

**行动指南**

项目开始前填写：

- **目标结果**：用户完成什么，而不是系统包含什么组件；
- **最低成功证据**：真实任务成功次数、持续运行时间、正确率或用户确认；
- **不可接受结果**：误报、漏报、延迟、成本或运维上限；
- **结束条件**：满足哪些证据后停止扩展并转入维护；
- **探索预算**：证据不足时最多投入多少时间、Token 或实现范围；
- **重新立项条件**：哪些新增需求必须重新评估架构，而非顺手加入。

**适用条件**

- 个人项目、AI 辅助项目、探索性产品和需求持续变化的系统；
- 技术优化容易取代用户目标的项目。

**例外与限制**

- 研究项目可能无法预先定义结果，但仍可定义预算和阶段性学习目标。
- 成功指标可能随真实反馈改变；改变时应记录原因，而不是静默移动终点。
- 本原则是针对信息缺口的预防措施，不是对 WaveMonitor 范围管理失败的事实判断。

**验证方法**

- 任意时点都能回答项目处于探索、交付还是维护阶段；
- 新需求可以根据成功标准判断接受、推迟或拒绝；
- 达到结束条件后，不再以无指标的“顺便优化”持续扩大范围。

---

## 3. 项目启动指南

### 3.1 项目启动卡

新项目在开始大规模实现前应填写以下内容：

```text
目标用户：
用户要完成的核心任务：
当前替代方案及其不足：

最小端到端结果：
最低成功证据：
可接受的延迟、错误率和恢复时间：
项目结束或转维护的条件：

明确不做：
当前部署规模：
当前数据规模：
必须保留的数据：
允许在重启时丢失的数据：

失败即淘汰的外部约束：
每个约束的最小验证实验：
实验预算与停止条件：

未来升级触发器：
本阶段不为哪些假设预留架构：
```

### 3.2 推荐启动顺序

1. **目标切片**：选出一个真实用户结果，而非完整功能列表。
2. **约束实验**：验证网络、平台、外部服务、持久化和安全边界。
3. **纵向 Baseline**：用最少组件贯通真实输入到真实结果。
4. **可观察性**：记录成功、失败、延迟和关键状态，而不是先建设复杂平台。
5. **真实运行**：让 Baseline 在目标环境运行，收集误报、漏报和操作成本。
6. **按证据扩展**：只处理已经出现的问题或已确认需求。

### 3.3 Baseline 的最低要求

Baseline 应当：

- 使用真实目标运行环境；
- 至少连接一个真实外部依赖；
- 产生用户能判断价值的输出；
- 有失败可见性和基本恢复路径；
- 有测试保护核心领域规则；
- 明确不支持的规模和可靠性边界。

Baseline 不要求：

- 为尚不存在的多租户或高可用需求设计；
- 同时实现多个运行时；
- 建立通用插件系统或自建框架；
- 保存尚无查询用途的所有原始数据；
- 把每段逻辑抽成独立模块。

---

## 4. 技术决策框架

### 4.1 决策前的五个问题

1. **要解决的可观察问题是什么？**
2. **不做这个决策，当前系统会在哪里失败？**
3. **最小实验能否先验证关键假设？**
4. **它会增加几个新的运行时、状态源、同步边界或长期维护面？**
5. **如果方案失败，如何退出并删除遗留结构？**

如果第 1、2 项无法回答，默认不引入；如果第 3 项尚未执行，不进入完整实现；如果第 5 项无法回答，先缩小方案。

### 4.2 决策记录模板

```markdown
# 决策名称

## 当前事实
- 已观察到的问题：
- 当前指标或用户反馈：

## 推断与未知
- 我们认为原因是：
- 尚未验证的是：

## 致命约束
- 约束：
- 验证实验：
- 通过/失败标准：

## 方案比较
- 保持现状：收益、代价
- 最小改动：收益、代价
- 新方案：收益、代价

## 决策
- 采用：
- 不采用：
- 适用边界：

## 退出与复审
- 旧方案删除项：
- 重新评估触发器：
- 验证指标：
```

### 4.3 引入第二套技术实现的额外门槛

若计划为同一业务增加第二后端、第二数据库或第二部署运行时，必须先回答：

- 新实现解决的是已验证问题，还是仅提供另一种部署偏好？
- 是否通过了所有致命约束实验？
- 哪些业务规则、契约和迁移逻辑会被复制？
- 如何自动检测两个实现的行为漂移？
- 谁承担双实现的缺陷修复和发布同步？
- 能否以独立仓库、共享契约测试或更小的适配层降低耦合？
- 何时停止其中一个实现？

没有明确答案时，不把实验性第二实现合入主线。

---

## 5. 开发与迭代策略

### 5.1 一次迭代只改变一个主要假设

尽量分开进行：

1. 保持行为的重构；
2. 业务语义变化；
3. 存储或运行时迁移；
4. 性能优化。

这样才能判断收益来自哪里，也能避免在需求改变时把旧结构一并保留下来。

### 5.2 用真实反馈修正规则，但先更新模型

真实运行反馈优先于最初设想。收到重复告警、漏报或错误状态后：

1. 将现象还原为输入、前置状态和期望转换；
2. 判断是需求变化还是实现偏差；
3. 更新状态表或领域规则；
4. 选择能区分新旧语义的回归样本；
5. 修改实现；
6. 删除被取代的状态和分支。

不要直接增加一个时间戳、布尔字段或特殊分支来压住现象。

### 5.3 优化前先建立最小指标

至少选择一个与问题直接相关的指标：

| 优化目标 | 最低证据 |
| --- | --- |
| 降低数据库压力 | 写入次数、文件增长、锁等待或查询延迟 |
| 降低网络成本 | 请求数、配额、失败率或账单 |
| 改善告警质量 | 重复率、误报率、漏报样本或用户确认 |
| 减少维护成本 | 修改涉及模块数、缺陷回归数或评审时间 |
| 降低 AI 消耗 | 相同任务的 Token、轮次或返工次数 |

没有基线指标时，可以做简化，但应写成“复杂度优化判断”，不能声称已证明性能改善。

### 5.4 每次替换后的关闭流程

```text
[ ] 新路径已覆盖目标行为
[ ] 旧路径已无生产调用
[ ] 旧状态、表、索引和配置已处理
[ ] 旧测试已删除或改为验证新语义
[ ] 用户文档和 API 契约已更新
[ ] 兼容项有明确期限
[ ] 不可逆数据删除已有备份说明
```

---

## 6. AI 辅助开发与资源管理

### 6.1 给 AI 的任务必须包含所有权和删除要求

除功能目标外，每次任务应说明：

- 行为由哪个现有模块拥有；
- 优先修改现有所有者，而不是新建 wrapper；
- 如果替换方案，要追踪并删除旧引用；
- 哪些边界必须保留；
- 如何验证结果而不只验证“没有报错”。

### 6.2 AI 变更评审四问

1. **新增了多少概念，而不只是新增了多少代码？**
2. **每个新层是否有独立职责或真实复用？**
3. **旧方案是否仍以兼容、保险或未来用途为名残留？**
4. **测试是在验证业务结果，还是复述实现结构？**

### 6.3 定期消融，而非事后大扫除

在每个功能里程碑结束时检查：

- 单调用方且只转发参数的 helper；
- 只包装框架原语的 hook；
- 已无订阅者的全局 store；
- 与当前运行路径脱离的模型、表和迁移；
- 重复表达同一类型或状态的模块；
- 为未来扩展创建但没有当前使用者的接口。

发现冗余时优先在当前上下文中删除，因为此时最清楚哪些方案已经被取代。不要等到代码含义遗忘后再进行大规模清理。

### 6.4 Token 与计算资源策略

- 先用直接搜索和小范围实验建立事实，再让 AI 完整实现；
- 不为同一个未验证方向同时生成后端、前端、迁移和文档；
- 对风险最高的假设设置固定探索预算；
- 避免让多个代理重复探索同一所有权路径；
- 记录返工原因：需求变化、事实未验证、实现错误或冗余设计；
- 只有连续记录后，才能声称某种流程节省了 Token。

本项目没有可靠的 Token 统计，因此这些是未来应验证的管理方法，不是已量化收益。

---

## 7. 常见陷阱与失败模式

### 7.1 先造完整系统，再验证平台能否运行

**早期信号**：已经讨论数据库迁移和设置页面，却没有从目标平台访问关键外部服务。

**应对**：暂停功能开发，执行致命约束实验；失败则立即终止或隔离实验分支。

### 7.2 用唯一键或 cooldown 代替业务状态机

**早期信号**：每出现一次重复或漏报，就增加时间戳、布尔值或唯一约束。

**应对**：先画状态转换表，区分业务事件、投递和 re-arm。

### 7.3 新旧方案同时存活

**早期信号**：新路径已上线，但旧表仍创建、旧 helper 仍测试、旧配置仍文档化。

**应对**：把删除项纳入替换变更的完成定义。

### 7.4 把主观简化描述为性能优化

**早期信号**：只有“感觉更快、更省”，没有改动前指标。

**应对**：将结论降级为复盘判断；补测指标，或只声明结构简化。

### 7.5 AI 为局部整洁持续增加全局概念

**早期信号**：大量 type-only 文件、单用途 hook、wrapper 和只有一个实现的接口。

**应对**：要求说明变化轴和所有权；执行消融检查。

### 7.6 把当前规模下成功的机制当成普遍答案

**早期信号**：因为 SQLite、自建迁移或单进程“目前没出问题”，就拒绝定义升级条件。

**应对**：保留简单方案，同时记录容量边界和复审触发器。

### 7.7 ADR 已接受，但实现仍是另一种行为

**早期信号**：ADR、测试和用户文档对失败重试、边界值或数据保留给出不同答案。

**应对**：标记“实现待对齐”，禁止把目标语义描述为现有保证。

---

## 8. 项目检查清单

### 8.1 项目启动

```text
[ ] 已写出用户结果，而不只是功能列表
[ ] 已定义最低成功证据和结束条件
[ ] 已明确当前用户、负载、可靠性和部署边界
[ ] 已明确本阶段不做什么
[ ] 已列出失败即淘汰的外部约束
[ ] 每个致命约束都有最小实验和停止条件
[ ] 已定义探索时间、Token 或实现范围预算
[ ] 已选择最小端到端 Baseline
```

### 8.2 技术选型

```text
[ ] 选型对应一个可观察问题
[ ] 保持现状和最小改动被纳入比较
[ ] 已在真实目标环境验证关键能力
[ ] 没有先实现第二套完整业务系统
[ ] 已评估新增运行时、状态源和同步成本
[ ] 已写明适用边界和升级触发器
[ ] 已设计失败后的退出与清理路径
```

### 8.3 开发迭代

```text
[ ] 本次变化属于重构、需求、迁移或优化中的哪一类
[ ] 行为变化已有能区分新旧语义的测试
[ ] 状态型业务已更新状态转换表
[ ] 新抽象拥有独立职责或真实变化轴
[ ] 新方案替换的旧路径已列出
[ ] 指标不足的收益被标为判断或假设
[ ] ADR、契约、代码、测试和用户文档保持一致
```

### 8.4 AI 代码评审

```text
[ ] AI 修改了现有所有者，而非无理由新增一层
[ ] 每个 helper、hook、store 和接口都有必要性说明
[ ] 没有保留已被替代的实现作为无期限保险
[ ] 测试验证业务结果，不只验证内部调用
[ ] 已搜索旧符号、旧表和旧配置的残留
[ ] 已执行一次小范围消融检查
```

### 8.5 发布与项目结束

```text
[ ] 目标运行环境已完成真实端到端验证
[ ] 已知实现偏差已公开记录
[ ] 破坏性迁移已有备份和恢复说明
[ ] 成功标准已经满足，或明确说明未满足项
[ ] 未完成想法已从当前范围移出，而非半接线保留
[ ] 项目已明确进入继续探索、功能交付或维护阶段
[ ] 复盘区分事实、个人判断和待验证假设
```

---

## 9. 证据与适用范围

### 9.1 ADR 证据矩阵

| 来源 | 支持的经验 | 证据边界 |
| --- | --- | --- |
| [ADR 0001](docs/adr/0001-single-instance-full-stack-and-polling.md) | 单实例 Baseline、轮询优先、明确非目标 | 没有与 PostgreSQL 或其他栈的对照评估 |
| [ADR 0002](docs/adr/0002-market-data-adapters-and-provider-catalogs.md) | 在真实外部差异处建立适配边界；提供降级路径 | 不能证明 SDK 方案优于直接 HTTP |
| [ADR 0003](docs/adr/0003-instrument-owned-source-mappings.md) | 按业务所有权建模；配置更新保持原子性 | 尚未证明共享抓取优化有必要 |
| [ADR 0004](docs/adr/0004-optional-shared-password-authentication.md) | 用与产品边界匹配的最低访问控制，避免擅自扩展账户体系 | 当前仍待复审；反向代理认证方向未验证 |
| [ADR 0005](docs/adr/0005-application-managed-sqlite-migrations.md) | 简单自建迁移可在有限规模工作；必须有升级测试和复审触发器 | 未与版本化迁移工具进行对照 |
| [ADR 0006](docs/adr/0006-multi-level-price-rules.md) | 业务扩展时保持纯规则核心；事件保存触发快照 | 多级价格与固定回撤是产品特定语义 |
| [ADR 0007](docs/adr/0007-persistent-alert-state-machine.md) | 告警状态机、reset/re-arm、规则周期和投递语义 | 失败重试仍未与当前实现对齐；不支持多实例 exactly-once |
| [ADR 0008](docs/adr/0008-in-process-monitoring-lifecycle.md) | 简单部署边界、同步 SDK 隔离、按触发器升级 | API 与调度仍共享故障域 |
| [ADR 0009](docs/adr/0009-frontend-api-validation-and-polling.md) | API 边界校验；数据所有权放在实际消费者；删除死 store/hook | 两分钟陈旧仅由所有者接受，未量化用户影响 |
| [ADR 0010](docs/adr/0010-cloudflare-workers-experiment.md) | 致命约束先验证；避免未经验证的双运行时 | 最佳最小实验形式仍不确定 |
| [ADR 0011](docs/adr/0011-in-memory-latest-price-state.md) | 按恢复需求划分瞬时与持久状态 | 性能收益没有指标证明 |
| [ADR 0012](docs/adr/0012-avoid-redundant-thin-layers.md) | 抽象需有独立职责；AI 代码需要消融检查 | 不证明所有小模块都应内联 |
| [ADR 0013](docs/adr/0013-remove-legacy-price-observation-storage.md) | 替换方案后删除旧模型、表、索引和测试 | 删除历史数据是当前产品边界，不可普遍推广 |

### 9.2 项目所有者确认的事实

- Cloudflare 方向跳过了前置可行性实验。
- 告警规则变化主要来自需求变化；持续条件导致重复告警是真实运行反馈。
- 后期膨胀主要与历史方案没有及时回收有关，并包含 AI 开发累积的代码。
- 行情最新状态转入内存没有量化性能依据，主要是主观优化判断。
- 项目实际运行过；目前没有认为核心技术栈必须更换。
- 应用自行管理 SQLite 迁移尚未造成明显问题。
- 各组件分别的长期可靠性、项目明确结束条件和其他低价值投入目前无法确认。

### 9.3 不应从本项目推出的结论

- 不能推出 SQLite、轮询或单进程总是优于更复杂架构。
- 不能推出 Cloudflare Workers 不适合行情应用；只能确认它不满足本项目当时的代理要求。
- 不能推出内存状态一定比数据库持久化更高效。
- 不能推出手写迁移优于 Alembic 等工具。
- 不能推出所有小模块、hook、store 或接口都是 AI 膨胀。
- 不能推出提前写出状态机可以消除真实运行带来的需求变化。
- 不能推出本项目已经建立了完整的成功指标或资源投入数据。

### 9.4 未来需要验证的假设

1. 致命约束实验能否显著减少被整体撤回的实现投入。
2. 抽象预算和里程碑消融检查能否减少 AI 返工与 Token 消耗。
3. 明确结束条件能否减少无指标优化和范围漂移。
4. 为告警预先维护状态转换表能否降低需求变化时的改动范围。
5. 记录持久化用途和恢复边界能否减少无人使用的数据结构。

验证这些假设时，应在未来项目中记录改动轮次、返工原因、概念数量、实际指标和用户反馈；在获得数据前继续保留“待验证”标记。

---

## 10. 最短执行版

如果未来项目只能保留本文的一页内容，请执行以下十条：

1. 先写用户结果、成功标准、结束条件和明确非目标。
2. 找出失败即淘汰的外部约束，先做真实环境实验。
3. 用最少组件打通一个真实端到端 Baseline。
4. 不为没有达到触发条件的规模提前分布式化。
5. 把跨时间业务写成状态转换，不用零散字段修补。
6. 新方案合入时同时删除旧运行路径和旧数据结构。
7. 只在真实变化边界建立抽象；AI 新增层必须说明职责。
8. 持久化数据前说明恢复、审计或查询用途。
9. 没有前后指标时，只能声称简化，不能声称性能提升。
10. ADR、实现、测试和运行结果一致后，决策才算完成。
