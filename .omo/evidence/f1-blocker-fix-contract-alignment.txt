# F1 Blocker Fix: Contract Alignment

## Changed Files
- `frontend/src/api/client.ts`: Updated Zod schemas (`InstrumentSchema`, `SourceMappingSchema`, `CreateInstrumentRequestSchema`) to use `near_support_threshold`, `risk_reward_threshold`, and `source_mappings`.
- `frontend/src/utils/validation.ts`: Added `validateRiskRewardThreshold` and updated `validateThreshold` to accept a name parameter.
- `frontend/src/pages/instruments/InstrumentForm.tsx`: Updated form state and inputs to use `near_support_threshold`, `risk_reward_threshold`, and `source_mappings`. Updated provider/market_type options to match backend canonical enums (`equity`, `usd_m_futures`, `coin_m_futures`, `perpetual`).
- `frontend/src/pages/instruments/InstrumentList.tsx`: Updated table columns to display `near_support_threshold`, `risk_reward_threshold`, and `source_mappings.length`.
- `frontend/src/pages/dashboard/Dashboard.tsx`: Updated `getSourceName` to use `source_mappings` instead of `mappings`.
- `frontend/src/tests/Instruments.test.tsx`: Updated mock data and form tests to expect the new schema fields.
- `frontend/tests/Dashboard.test.tsx`: Updated mock data to use the new schema fields.

## RED Failure
Tests failed initially because the frontend components and API client were using stale fields (`threshold`, `mappings`, `spot`, `perp`, `usd_m`, `coin_m`) while the tests expected the new schema (`near_support_threshold`, `risk_reward_threshold`, `source_mappings`).

## GREEN Results
After updating the frontend code to match the backend schema, all tests passed:
```
 Test Files  6 passed (6)
      Tests  34 passed (34)
```
Typecheck (`npm run typecheck`) and build (`npm run build`) also passed successfully.

## Manual Proof
A manual QA test (`frontend/src/tests/ManualQA.test.ts`) was run to prove that `createInstrument` sends the canonical backend JSON and handles a backend-style response. The test passed, verifying the payload matches the backend schema:
```json
{
  "name": "Test Instrument",
  "enabled": true,
  "support": "100.0",
  "resistance": "200.0",
  "near_support_threshold": "0.05",
  "risk_reward_threshold": "2.0",
  "source_mappings": [
    {
      "provider": "binance",
      "market_type": "usd_m_futures",
      "symbol": "BTCUSDT",
      "enabled": true
    }
  ]
}
```

## Cleanup
Removed temporary manual QA script (`frontend/manual_qa.ts`).
