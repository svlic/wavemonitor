DoneClaim: Todo 2 backend configuration, domain models, and Decimal-safe rule contracts
Date: Tue Jun 30 2026

changed_files:
- pyproject.toml
  - Added minimal sqlmodel dependency for SQLModel/SQLAlchemy-backed domain tables and schema inspection.
- backend/src/wavemonitor_backend/app.py
  - Wired FastAPI scaffold to Settings.from_env() for Telegram readiness without exposing credentials.
- backend/src/wavemonitor_backend/models.py
  - Added SQLModel models for Instrument, SourceMapping, PriceObservation, AlertEvent, and LastRuleState.
  - Added Provider, MarketType, and AlertKind typed enums.
  - Enforced string/Decimal/int-only Decimal parsing and rejection of float rule inputs.
  - Enforced support < resistance, near-support threshold as decimal fraction, positive risk/reward threshold.
  - Added stable SourceMapping identity provider + market type + normalized symbol.
  - No Telegram token/chat-id columns or fields are persisted in DB models.
- backend/src/wavemonitor_backend/settings.py
  - Added typed Settings loaded from TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID environment variables only.
  - Added telegram_ready and public_status() redacted readiness surface.
- backend/tests/test_models.py
  - Added TDD tests for Decimal parsing, float rejection, invalid support/resistance, one support/resistance pair, source identity, persisted Decimal event/state values, and schema secret absence.
- backend/tests/test_settings.py
  - Added TDD tests for missing env readiness false, both env vars readiness true, and public dump redaction.

RED tests:
1. Initial RED before dependency install:
   command: .venv/bin/pytest backend/tests/test_models.py backend/tests/test_settings.py
   result: collection failed because sqlalchemy/sqlmodel were not installed and settings module was missing.
   note: Added minimal sqlmodel dependency, then re-ran to isolate product-code RED.
2. Product-code RED after dependency install:
   command: .venv/bin/pytest backend/tests/test_models.py backend/tests/test_settings.py
   result: failed during collection with ModuleNotFoundError for wavemonitor_backend.models and wavemonitor_backend.settings.
   meaning: tests failed for missing Todo 2 behavior/modules, not for test syntax.

GREEN tests:
- command: .venv/bin/pytest backend/tests/test_models.py backend/tests/test_settings.py
  result: 10 passed in 0.31s
- command: .venv/bin/pytest backend/tests/test_models.py backend/tests/test_settings.py backend/tests/test_health.py -vv
  result: 12 passed, 1 warning in 0.56s
- command: .venv/bin/pytest backend/tests -vv
  result: 12 passed, 1 warning in 0.66s
  warning: FastAPI TestClient/Starlette deprecation says install httpx2; existing Todo 1 test stack warning, not a failure.
- command: .venv/bin/python -m compileall backend/src/wavemonitor_backend backend/tests
  result: compiled backend package and tests successfully.

manual_QA_artifact:
command:
.venv/bin/python - <<'PY'
from wavemonitor_backend.models import Instrument
from wavemonitor_backend.settings import Settings
import os

os.environ.pop("TELEGRAM_BOT_TOKEN", None)
os.environ.pop("TELEGRAM_CHAT_ID", None)
print("telegram_ready", Settings.from_env().telegram_ready)
try:
    Instrument(
        name="bad",
        support="100",
        resistance="100",
        near_support_threshold="0.02",
        risk_reward_threshold="2",
    )
except Exception as exc:
    print(type(exc).__name__)
    print(str(exc).splitlines()[0])
PY
output:
telegram_ready False
ValidationError
1 validation error for Instrument

acceptance_evidence:
- backend model/settings tests pass: yes, 10 passed targeted.
- schema inspection proves no Telegram token/chat id columns: yes, test_database_schema_does_not_persist_telegram_credentials passes against SQLModel.metadata-created SQLite schema.
- Decimal tests reject float-only arithmetic paths: yes, test_instrument_rejects_float_only_rule_inputs passes.
- exactly one support/resistance per instrument: yes, Instrument exposes scalar Decimal support and resistance only; tests assert no support_levels/resistance_levels attributes.
- thresholds as decimal fractions: yes, near_support_threshold validates 0 < value < 1 and tests parse "0.02" as Decimal("0.02").
- stable source mapping identity provider + market type + symbol: yes, SourceMapping.identity_key tested for binance:usd_m_futures:BTCUSDT and binance:coin_m_futures:BTCUSD_PERP.
- must not store Telegram credentials in DB: yes, DB models contain no Telegram/token/chat_id/bot_token columns and settings are env-only.

cleanup:
- No backend server, scheduler, database file, Docker service, frontend process, live provider, or Telegram call was started.
- No spawned long-running resources required cleanup.
- Python compileall created/updated __pycache__ files only.
- No commit made.

UltraQA:
- misleading_success_output: addressed by re-running verbose pytest when quiet output was compressed to a warning line; final pass evidence includes explicit 12 passed.
- dirty_worktree/scope: git status unavailable because /opt/ocode/wavemonitor is not a git repository; touched files are limited to allowed backend Python/tests/config plus this evidence artifact. No frontend edits. Plan checkboxes were not edited.
- stale_state: tests executed after implementation in current venv; dependency installed into local .venv with .venv/bin/python -m pip install -e '.[test]'.
- hung_or_long_commands: all commands completed under timeout; no hung processes observed.
- flaky_tests: no sleeps, wall-clock waits, live network, or external APIs in tests.

risks:
- Ruff is configured in pyproject.toml but not installed in the local venv; `.venv/bin/python -m ruff check ...` failed with `No module named ruff`. I did not add ruff because Todo 2 only required minimal dependency changes for SQLModel and tests/build currently pass.
- SQLModel table models are intentionally minimal domain/persistence contracts for Todo 2. CRUD APIs, migrations, adapters, scheduler, Telegram sender, and alert evaluation remain out of scope for later todos.
- Existing FastAPI TestClient stack emits a StarletteDeprecationWarning recommending httpx2; tests pass and no runtime behavior is affected.
